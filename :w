#include <iostream>
#include <math.h>
#include <vector>

using namespace std;

int main() {
  int T;
  int l, X, Y;
  int Q;

  cin >> T;
  cin >> l >> X >> Y;
  cin >> Q;

  vector<int> E(Q);

  for (int i = 0; i < Q; i++) {
    cin >> E[i];
  }

  for (int i = 0; i < Q; i++) {
    // E869120 君 の y 座標
    long long y_e =
  }
}
